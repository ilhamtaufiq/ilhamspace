# Coolify: Build Pack = Docker Compose.
# IMPORTANT: In Coolify UI, remove ALL "Persistent Storage" entries for /data.
# This file already defines ilhamspace-data:/data — extra Coolify storage breaks deploy.
services:
  ilhamspace:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ilhamspace-data:/data
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      ORIGIN: ${ORIGIN:-http://localhost:3000}
      DATABASE_PATH: /data/ilhamspace.db
      SESSION_SECRET: ${SESSION_SECRET:?Set SESSION_SECRET}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_NAME: ${ADMIN_NAME:-Admin}
      ADMIN_PASSWORD_HASH: ${ADMIN_PASSWORD_HASH:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      BODY_SIZE_LIMIT: ${BODY_SIZE_LIMIT:-15M}

volumes:
  ilhamspace-data: